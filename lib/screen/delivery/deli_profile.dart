import 'package:flutter/material.dart';

class DeliProfile extends StatelessWidget {
  const DeliProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Profile")));
  }
}
